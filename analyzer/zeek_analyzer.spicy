# This file sets up protocol confirmation/rejection for analyzers.

module Zeek_zeek_spicy_facefish;

import zeek_spicy_facefish;
import zeek;

# TODO: For DPD, protocol analyzers should confirm or reject a protocol.
# on zeek_spicy_facefish::FACEFISH::%done {
#     zeek::confirm_protocol();
# }
#
# on zeek_spicy_facefish::FACEFISH::%error {
#     zeek::reject_protocol("error while parsing FACEFISH record");
# }
